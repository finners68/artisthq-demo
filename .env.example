# Supabase credentials for LockleadHQ
# Copy values into js/config.js (this static app has no build step).

SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key

# Run the SQL migration in supabase/migrations/001_initial_schema.sql
# via the Supabase SQL editor or CLI before first use.
